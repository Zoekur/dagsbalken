package com.dagsbalken.app.ui.settings

import androidx.compose.runtime.Composable

// Placeholder for future timer editor UI. Currently unused but kept so references compile.
@Composable
fun TimerEditor() {
    // TODO: Implement timer editor UI when timers feature is designed.
}

