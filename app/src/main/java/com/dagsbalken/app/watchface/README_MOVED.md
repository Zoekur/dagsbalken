This package has moved.

The Dagsbalken Wear OS watch face is now implemented in the separate Gradle module:

- :wear-watchface

This folder is kept temporarily to avoid confusing git history, but the code should not be referenced from the phone app.
